<?php
if (isset($rates) && count($rates))
{
  echo $html->selectTag('Project/rate_id', $rates, $html->tagValue('Project/rate_id'), array('id'=>'input_rate_id'));
}
else
{
  echo '<select name="data[Project][rate_id]" id="input_rate_id"><option value=""></option></select>';
}
?>